import { test, expect } from "@playwright/test";

/**
 * Placeholder E2E test. Replace with real specs as features are built.
 * Run against a live dev server: npm run dev (then npm run test:e2e)
 */
test.describe("example E2E test", () => {
  test("placeholder — always passes", async () => {
    expect(true).toBe(true);
  });
});
